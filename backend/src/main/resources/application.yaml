server:
  port: 9090

logging:
  level:
    org.springframework.security: DEBUG

spring:
  application:
    name: backend
  security:
    user:
      name: user
      password: password

  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update

  h2:
    console:
      enabled: true
